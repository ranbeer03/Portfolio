import React from 'react';
import './LoadingBar.css'; // We will create this file for styling.

const LoadingBar = () => {
  return (
        <div className="container">
            <div className="Loading"></div>
        </div>
  );
}

export default LoadingBar;
